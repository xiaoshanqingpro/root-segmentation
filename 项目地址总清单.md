# 项目地址总清单

收集时间：2026-09-24 ｜ 收集脚本：`code\69_collect_locations.py`

---

## 一句话

**项目本体在 `D:\根系分割项目\`（2254 文件 / 67.05 GB）；原始扫描数据仍在 C 盘桌面。**

2026-09-23 22:05 用 ROBOCOPY `/MOVE` 从 C 盘搬到 D 盘（C 盘当时只剩 17GB）。
`code\paths.py` 已同步更新，**新脚本一律走 `paths.py`，不要硬编码路径**。

---

## 一、项目本体：`D:\根系分割项目\`

| 子目录 | 文件 | 体积 | 内容 |
|---|---|---|---|
| `机器图_CV\` | 442 | **42.53 GB** | 经典规则管线产出（**纯黑白二值**） |
| `机器图_UNet\` | 333 | **15.23 GB** | U-Net 产出（**保留原色的白底图**） |
| `outputs\` | 874 | 7.33 GB | 中间产物 / 评估 / 对比图 / 训练权重与日志 |
| `processed\` | 108 | 0.83 GB | 数据集1 预处理（已裁边 + 统一 600dpi） |
| `白底根图_第二数据集\` | 180 | 0.33 GB | 数据集2 旧版白底图 |
| `白底根图\` | 106 | 0.17 GB | 数据集1 旧版白底图 |
| `models\` | 1 | 0.35 GB | SAM v1 权重 `sam_vit_b_01ec64.pth` |
| `code\` | 114 | — | 全部脚本（00→69 编号） |
| `docs\` | 20 | — | 全部文档 |
| `labels\` | 10 | — | SAM 三分类候选标签 |
| `config\` | 1 | — | 阶段二工作集清单 |
| `_code_backup_20260923\` | 59 | — | 代码备份 |

**关键文件**
- 训练权重：`outputs\训练\best_unet.pt`（val dice 0.9004）
- 判根实现：`code\rootseg_common.py`（CV 管线唯一实现）
- 路径总控：`code\paths.py`
- 预标注工具：`code\49_preannotate.py`
- 边框清理：`code\borderclean.py`
- 目录变更说明：`目录变更说明_20260924.md`

### `机器图_CV\` 结构

```
机器图_CV\
├─ 数据集1\      106   (0807 41 / 0808 12 / 0811 39 / 0814 12 / 散图 2)
├─ 数据集2\      180   (9.15 30 / 9.8校准 35 / 多样性林 115)
└─ 杉阔混交林\    151   (9.8校准 35 / F 92 / F补 24)
```

### `机器图_UNet\` 结构

```
机器图_UNet\
├─ 数据集1\       26   ← 缺 80 张
├─ 数据集2\      154   ← 缺 26 张
└─ 杉阔混交林\    151   ← 齐全
```

> ⚠️ **U-Net 覆盖不全**。数据集1/2 那 180 张原本是"预标注候选"（给人工标注做初稿），
> 不是正式全量交付。要补齐需再跑 106 + 26 张。

---

## 二、原始数据（仍在 C 盘桌面）

| 位置 | 文件 | 体积 | 内容 |
|---|---|---|---|
| `C:\Users\LIHAOYANG\Desktop\扫描\图片\` | 109 | 0.41 GB | **数据集1**，纵向 4962×7019，600dpi |
| `C:\Users\LIHAOYANG\Desktop\扫描\原始图片\` | 212 | 6.18 GB | **数据集2**，横向 7019×4962，600dpi |
| `C:\Users\LIHAOYANG\Desktop\杉阔混交林\` | 151 | 0.97 GB | **杉阔混交林**，纵横混合 + 14 张 300dpi |

## 三、真值 / 测试区（C 盘）

`C:\Users\LIHAOYANG\Desktop\测试\` — 55 文件 / 4.18 GB

| 子目录 | 文件 | 体积 | 内容 |
|---|---|---|---|
| `人工\` | 15 | 2.13 GB | **人工标注（`-man`）= 训练标签来源** |
| `机器\` | 21 | 1.76 GB | 机器图（`-mac`） |
| `原图\` | 11 | 0.29 GB | 测试用原图 |
| `测试数据.TXT` | 1 | — | **WinRHIZO 端到端扫描数据**（12 对） |

## 四、历史 / 残留（可清理）

| 位置 | 文件 | 体积 | 说明 |
|---|---|---|---|
| `C:\Users\LIHAOYANG\Desktop\采样数据\` | 124 | 0.02 GB | 图片已移走，只剩一套 OCR→Excel 脚本（与本项目无关） |
| `C:\Users\LIHAOYANG\Desktop\扫描\根系分割项目\` | 25 | 0.02 GB | **搬迁后残留在 C 盘的旧地址**，含另一条线写的 `outputs\对比十张_评估\` |
| `C:\Users\LIHAOYANG\Desktop\扫描\rootpipe\` | — | — | 早期自动化尝试 |
| `C:\Users\LIHAOYANG\Desktop\扫描\自动化尝试\` | — | — | 早期自动扫描尝试 |

## 五、磁盘

| 盘 | 已用 | 可用 |
|---|---|---|
| C: | 342.4 GB | **57.6 GB** |
| D: | 504.5 GB | **47.1 GB** |

---

## 六、两个必须知道的口径差异

**1. 着色方式不同**
- `机器图_CV\` = **纯黑白二值**（2 个灰阶，根 = 0 黑）
- `机器图_UNet\` = **保留原色的白底图**（约 246 个灰阶，根保留褐色）

做像素比对时**统一按"非白像素"统计**，别直接比灰度值。

**2. 文件名后缀不统一**
- `机器图_UNet\杉阔混交林\` → `<样本>-mac.tif`
- `机器图_UNet\数据集1|数据集2\` → `<样本>-unet.tif`
- 按**样本名**匹配即可（改名会破坏 `batch_unet_report.csv` 等记录）

---

## 七、⚠️ 已知未修缺陷

`rootseg_common.py` 的 `EDGE_KILL` 规则会**误杀"顶到画面边缘且跨度 ≥ 画幅 25%"的真根**。

- 最典型：`杉阔混交林\F补\23-B-GG-002` —— 判根层本检出 154,341 px 的好根，被这条规则删到只剩 **4,718 px**
- 三个数据集都受影响（抽样 40 张，单张最高被删 42%）
- 详见 `机器图_CV\README.md` 第四节
- **目前保持原样未修**

修法：把 `EDGE_SPAN` 从 0.25 调大、或改成"贴边 + 整体偏亮"双重判据（`borderclean.py` 用的是后者，更安全）。
