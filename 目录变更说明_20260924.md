# 目录变更说明 · 机器图重组

日期：2026-09-24 ｜ 执行脚本：`code\68_reorg_cv_unet.py`

---

## 为什么动

原来两套产出散在三个地方、命名也不一致：

| 位置 | 内容 |
|---|---|
| `机器图\` | CV，数据集1+2（286 张） |
| `机器图_杉阔混交林\` | CV，杉阔混交林（151 张） |
| `机器图_杉阔混交林_unet\` | U-Net，杉阔混交林（151 张） |
| `outputs\预标注候选\*-unet.tif` | U-Net，数据集1/2（180 张） |

按你的要求，**分成两个顶层文件夹，各自保持原有组织结构**。

---

## 现在长什么样

```
D:\根系分割项目\
├─ 机器图_CV\          437 张 / 42.53 GB
│   ├─ 数据集1\  106     （0807 41 / 0808 12 / 0811 39 / 0814 12 / 散图 2）
│   ├─ 数据集2\  180     （9.15 30 / 9.8校准 35 / 多样性林 115）
│   ├─ 杉阔混交林\ 151    （9.8校准 35 / F 92 / F补 24）
│   ├─ README.md
│   ├─ README_数据集12.md         （原 机器图\README.md，内容未改）
│   ├─ _批量清单_数据集12.csv
│   ├─ _跳过清单_数据集12.csv
│   └─ 杉阔混交林\_批量清单.csv
│
└─ 机器图_UNet\        331 张 / 15.23 GB
    ├─ 数据集1\  26      （0807 14 / 0808 6 / 0814 6）
    ├─ 数据集2\  154     （9.15 30 / 9.8校准 35 / 多样性林 89）
    ├─ 杉阔混交林\ 151    （9.8校准 35 / F 92 / F补 24）+ batch_unet_report.csv
    └─ README.md
```

---

## 怎么动的

- **同盘移动**（`shutil.move`，瞬时重命名），**没有复制、没有删除任何一张图**
- **没有改任何文件名**
- 只清理了 3 个变空的旧目录：`机器图\`、`机器图_杉阔混交林\`、`机器图_杉阔混交林_unet\`
- `outputs\预标注候选\` 里的 `candidates.csv`、`preannotate.log` **留在原处**，
  只有 180 个 `*-unet.tif` 被移走

---

## 两个必须知道的口径差异

**1. 着色方式不同**
- CV 产出 = **纯黑白二值**（2 个灰阶，根 = 0 黑）
- U-Net 产出 = **保留原色的白底图**（约 246 个灰阶，根保留褐色）

做像素比对时请统一按"非白像素"统计，别直接比灰度值。

**2. 文件名后缀不统一**（刻意未改）
- `机器图_UNet\杉阔混交林\...` → `<样本>-mac.tif`
- `机器图_UNet\数据集1|数据集2\...` → `<样本>-unet.tif`

改名会破坏 `batch_unet_report.csv` 和 `outputs\预标注候选\` 里的记录，所以保持原样。
按样本名匹配即可。

---

## 覆盖缺口（要看数据的人必须知道）

| 数据集 | CV | U-Net | 缺口 |
|---|---|---|---|
| 数据集1 | 106 | **26** | 80 张没有 U-Net 版 |
| 数据集2 | 180 | **154** | 26 张没有 U-Net 版 |
| 杉阔混交林 | 151 | **151** | 无 |

U-Net 在数据集1/2 的那 180 张原本是"预标注候选"（给人工标注做初稿用的），
不是正式全量交付。要补齐数据集1/2 需再跑 106 张。

---

## 三方对比图在哪

`D:\根系分割项目\outputs\对比十张\`
- `十张_原图_CV_UNet_对比.png` —— 10 行总览（1812×7675）
- `单张\01…10_三方.png` —— 每张单独一张，分辨率更高，方便放大看
- `_抽样清单.csv` —— 抽到哪 10 张、各自根占比、UNet/CV 倍数

抽样方式：从 CV 与 U-Net 都有的 262 个样本里随机抽 10 张（`random.seed(20260924)`，可复现）。

---

## ⚠️ CV 端一个未修的缺陷

`rootseg_common.py` 的 `EDGE_KILL` 规则会误杀"顶到画面边缘且跨度 ≥ 画幅 25%"的真根。
`杉阔混交林\F补\23-B-GG-002` 是最典型的例子：判根层本来完整检出了 154,341 px 的好根，
被这条规则删到只剩 4,718 px。

**三个数据集都受影响**（抽样 40 张，最高单张被删 42%）。
详见 `机器图_CV\README.md` 第四节。这个缺陷目前**保持原样未修**。
